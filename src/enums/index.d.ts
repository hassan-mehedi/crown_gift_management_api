export enum UserRole {
    ADMIN = "admin",
    USER = "user",
    RECEIVER = "receiver",
}

export enum ApprovalStatus {
    PENDING = "pending",
    APPROVED = "approved",
    CHANGE_REQUESTED = "change_requested",
    REJECTED = "rejected",
}
